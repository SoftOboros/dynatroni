# PostgreSQL Client Authentication Configuration File
# Managed by softoboros - changes here will be overwritten on boot
#
# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Local connections
local   all             postgres                                peer
local   all             all                                     scram-sha-256

# Localhost replication (for Patroni health checks)
host    replication     replicator      127.0.0.1/32            scram-sha-256
host    replication     replicator      ::1/128                 scram-sha-256

# VPC replication (Patroni cluster members)
host    replication     replicator      10.20.0.0/16            scram-sha-256

# VPC application connections
host    all             all             10.20.0.0/16            scram-sha-256

# External connections (through pgbouncer/NLB)
host    all             all             0.0.0.0/0               scram-sha-256
